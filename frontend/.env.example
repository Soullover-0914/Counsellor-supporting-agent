# Client-safe configuration only. Never place secrets here.
# Leave empty to use the Vite proxy in development (recommended).
# Production (Vercel) is set in frontend/.env.production.
VITE_API_BASE_URL=
